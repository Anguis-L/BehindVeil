# 开发文档（SDD / TDD 等），仅本地保留，不入库
00.development/

# 本地 AI 协作指令（AGENTS.md），仅个人使用，不入库
/AGENTS.md

# WorkBuddy 项目数据（会话记忆等），仅本地保留，不入库
.workbuddy/

# 依赖与构建产物
node_modules/
dist/
coverage/
*.tsbuildinfo

# 运行时数据（TDD §6：备份 = 复制 data/）
data/

# 环境变量（LLM key 等敏感配置，TDD §7 NFR-03）
.env
.env.*
!.env.example

# 日志与系统文件
*.log
.DS_Store
Thumbs.db
