# CODING_STANDARDS.md — BehindVeil 编码规则

> 本文件入库，是项目级共识。区别于根目录 `AGENTS.md`（.gitignore，纯本地个人 AI 协作偏好）。
> 与 `00.development/` 下的 SDD/TDD 互为补充：本文讲「怎么写代码」，SDD/TDD 讲「写什么代码」。

## 0. 适用范围

- 适用于 `packages/` 下所有源码（`server` / `shared` / `web`）。
- 不适用于 `dist/`、`coverage/`、`node_modules/`、`data/`，以及 `scripts/` 下的 CI 脚本与各类 `*.config.*` 文件。
- 本文与 `AGENTS.md` 冲突时，**以本文为准**（除非属于个人工作偏好范围）。

## 1. 行为原则（Karpathy 四条·项目化）

### 1.1 先想再写

不要靠假设推进。

- **假设要显式说出来**。不确定就问，不要默默选一边。
- 涉及架构或机制改动时，先对照 `00.development/` 下的 SDD/TDD；发现实现与文档冲突，**指出来讨论**，不要私自选边。
- 涉及**数值 / 判定 / 掷骰**相关代码时，先回答三个问题：
  1. 谁产生数值？（必须是服务端，不是 LLM）
  2. 用什么随机源？（必须是 CSPRNG）
  3. 怎么追溯？（必须带流水号 + 原始骰值，可复现）

  任一答不上来——停下来。

### 1.2 简洁优先

最小代码解决问题，不写推测性内容。

- 不实现 v1 明确不做的东西：战术地图与令牌、语音/立绘、万人级并发 SaaS、移动端原生 App。
- 不为「以后可能用到」加抽象层、配置项、参数。需要时再加。
- 不写不会发生的错误处理。
- 未到对应里程碑（M0–M6）不提前实现后续功能，防范围蔓延。
- 自检：「一个资深工程师会觉得这段过度设计吗？」觉得就重写。

### 1.3 外科手术式改动

只动你该动的。

- 不重构没坏的东西。
- 不顺手改相邻代码、注释、格式。
- 匹配现有风格，即便你有不同偏好。
- 发现无关死代码 / 待优化项 → **指出来，不要删/改**。
- 改动产生的孤儿（unused imports / 变量 / 函数）要清理；预先存在的死代码不动。
- 检验：每一行改动都能追溯到需求或 bug 报告。

### 1.4 目标驱动

定义可验证的成功标准，循环到通过为止。

| 模糊任务 | 可验证目标 |
|---|---|
| 加校验 | 写一个非法输入的测试，让它通过 |
| 修 bug | 写一个复现 bug 的测试，让它通过 |
| 重构 X | 改动前后测试都通过 |

多步任务先列计划：

```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
```

提交前跑一遍 `pnpm verify`（见 §3）。

## 2. 项目硬约束（不可妥协）

### 2.1 类型安全

- TypeScript `strict: true`，且 `noUncheckedIndexedAccess` / `noImplicitOverride` / `verbatimModuleSyntax` 全部启用（已在 `tsconfig.base.json` 落地，不要降级）。
- 前后端契约走 `@behindveil/shared` 的 Zod Schema，**类型从 schema 推导**（`z.infer<typeof ...>`），不手写重复类型。
- 不允许 `any`。确实无法避免时用 `unknown` + 显式窄化，并写注释说明原因。

### 2.2 服务端权威（核心不变式）

**叙事交给 LLM，规则交给代码。** 任何让 LLM 直接产生数值或判定结果的代码都是 bug。

- 掷骰：服务端 CSPRNG 生成，每次记录 [流水号, 骰式, 原始骰值数组, 求和/取舍结果, 时间戳]。
- 检定：服务端解析结构化指令块执行，LLM 只能请求检定，不能宣告结果。
- 流水号必须可复现：相同种子 + 相同请求 → 相同结果。

### 2.3 持久化

- 仅本地文件：JSONL（追加型日志）+ JSON（快照）。
- **不引入数据库**（含 SQLite）。备份 = 复制 `data/` 目录。
- 写文件先落临时文件再原子 rename，避免半写损坏。

### 2.4 依赖许可

- 依赖树出现 **AGPL / GPL** 即 CI 失败（NFR-08）。
- 新增依赖前跑 `pnpm check:licenses`（已包含在 `pnpm verify`）。
- 倾向 MIT / Apache-2.0 / BSD / ISC。

### 2.5 测试覆盖率

- 全量覆盖率 ≥ **85%**（NFR-09），由 `pnpm test:coverage` 检查。
- 数值 / 判定相关代码（掷骰、检定、流水号、Schema 解析）覆盖率目标 **100%**——这些是审计核心。
- 测试与源码同目录，命名 `*.test.ts`。

### 2.6 控制台输出

- 业务代码不鼓励 `console.log`（ESLint 当前 warn 级别）。
- 诊断走 `console.warn`，错误走 `console.error`。

## 3. 验证

一行命令检查上述大部分规则：

```bash
pnpm verify
```

等价于依次执行：`format:check` → `lint` → `build` → `typecheck` → `test:coverage`（≥85%）→ `check:licenses`（无 AGPL/GPL）。

任一环节失败 = 不允许合入 `main`。

## 4. 例外与豁免

- `scripts/` 下的 CI 脚本允许 `console.log`（已在 ESLint 配置中放行）。
- `*.config.ts` / `*.config.js` 被 ESLint 整体忽略。
- 真有正当理由需要绕过某条硬约束（如必须引入某 GPL 依赖），先在 `00.development/` 下写 ADR，列入设计文档讨论，再决定。